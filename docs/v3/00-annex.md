# Annex — n8n library (V3 note)

**V3 here is not a second JSON format.** It is a label for how you *present* the same workflows externally:

- **Technical + import truth:** `../v2/` in this folder, plus `CHANGELOG.md`.
- **Creative go-to-market skin:** AutoTagger `v5-workspace/playbooks/v3/` (Archival Annex HTML).

When writing customer-facing pages, you may use Annex tone and layout, but **commands, env keys, and provider URLs** must still match `docs/v2/` and the actual JSON nodes.
